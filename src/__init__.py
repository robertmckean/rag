"""Project source package."""

# The repo uses `src` as the local import root for all executable code.
# Keeping this package marker explicit helps tests and one-off CLI snippets resolve imports.
# Phase 1 logic remains organized beneath `src.rag` to avoid accidental layout drift.
