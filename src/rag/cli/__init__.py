"""CLI package for repository commands."""

# CLI modules are thin wrappers around library code in the neighboring packages.
# They should stay focused on argument parsing, calling one workflow, and printing results.
# This boundary keeps execution behavior easy to validate with targeted smoke checks.
