"""Source-specific parser package."""

# Phase 1 extraction logic currently lives under `normalize` rather than here.
# This package marker remains so future parser-specific code can be added without reshaping imports.
# Leaving the namespace in place is cheaper than deleting and reintroducing it later.
